<div class="mb-3">
    <label for="{{ id }}" class="form-label">{{ label }}</label>
    <input class="form-control" id="{{ id }}" type="{{ type }}" placeholder="{{ placeholder }}" required="{{ required }}">
</div>
