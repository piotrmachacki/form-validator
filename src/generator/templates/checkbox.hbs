<div class="mb-3">
    <div class="form-check">
        <input class="form-check-input" type="checkbox" value="{{ value }}" id="{{ id }}" {{#if autofocus}}autofocus{{/if}}>
        <label class="form-check-label" for="{{ id }}">
            {{ label }}
        </label>
    </div>
</div>
